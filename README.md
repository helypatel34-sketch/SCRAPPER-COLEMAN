SCRAPPER — Coleman Furniture
Private Coleman Furniture scraper project.

Goal
Discover public Coleman Furniture product pages recursively, collect:

Product Name
Product URL
Price
Then validate, deduplicate, batch-process, and produce final CSV outputs through GitHub Actions.

Important
This project uses normal public-page access and rate limiting. It does not bypass CAPTCHA, anti-bot challenges, or access controls.
